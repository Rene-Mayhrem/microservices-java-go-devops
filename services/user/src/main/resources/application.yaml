server:
  port: 8081 #? Each server has its own port

spring:
  application:
    name: user-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: user
    password: admin123
    driver-class-name: org.postgresql.Driver 

  jpa:
    hibernate:
      ddl-auto: update #? only for dev
    show-sql: true
    properties:
      hibernate:
        format_sql: true